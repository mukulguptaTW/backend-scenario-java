target/*
.settings/*
.classpath
.project
.idea
*.iml
/target

generated/

# Easier branch switching
springboot-petclinic-client/
springboot-petclinic-server/

# jenv local settings
.java-version

# Mac OS X indexing
.DS_Store

# asdf generated files
.tool-versions
.factorypath

# newman reports
./src/test/postman/reports/

# Secrets scan report — contains secret metadata; must never be committed
gitleaks-report.json

# Snyk vulnerability report — contains sensitive dependency metadata; must never be committed
snyk-report.json
