# ⚠️  THIS FILE IS INTENTIONALLY INSECURE — FOR DEVSECOPS TRAINING ONLY
# It simulates a developer accidentally committing secrets.
# The secret-scanner workflow in .github/workflows/secret-scanner.yml should detect these.

# Fake AWS credentials (not real — training use only)
aws.access.key.id=AKIAIOSFODNN7EXAMPLE
aws.secret.access.key=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY

# Fake database password hardcoded (not real — training use only)
spring.datasource.password=SuperSecret@1234!

# Fake GitHub token (not real — training use only)
github.token=ghp_FakeGitHubTokenForTraining1234567890AB

# Fake Stripe API key (not real — training use only)
stripe.api.key=sk_live_FakeStripeKeyForTrainingPurpose0000
